# ===============================
# = DATA SOURCE
# ===============================

spring.datasource.url = jdbc:mysql://g9fej9rujq0yt0cd.cbetxkdyhwsb.us-east-1.rds.amazonaws.com:3306/t325rmowvq6xgeyh?useSSL=false
spring.datasource.username = noze67b4949t4cf4
spring.datasource.password = wborabo1w3wh0fx5

#spring.datasource.url = jdbc:mysql://localhost:8889/schedulardb?useSSL=false
#spring.datasource.username = root
#spring.datasource.password = fadman4real

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#debug=true

logging.level.org.springframework=true
logging.level.com.quizzy.app=true

logging.level.org.hibernate.sql=debug
logging.level.org.hibernate.type=trace
